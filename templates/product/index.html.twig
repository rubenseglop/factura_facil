{% extends 'base.html.twig' %}

{% block title %}Mis Productos{% endblock %}
{% block stylesheets %}
        <style>
            .todos-Productos{

            }
            ul{
                list-style-type:none;
            }

        </style>
{% endblock %}
{% block body %}
<div class="todos-Productos">
    <form action="{{ path('product',{id:id_company}) }}" method="POST">
        <p>Search Product: </p><input type="text" name="name" id="name">
        <input type="submit" value="Submit">
    </form>

    <br>

    <h1 id="misProductos">My Products</h1>
    <br>
    <a href="{{ path('addProduct',{id:id_company}) }}"><button>Add New Product + </button></a>
    
    <ul>
       {% for p in products %}
       
            <li>{{p.name}}  ------>  <a href="{{ path('editProduct',{idProduct:p.id,id:id_company}) }}"><button>Editar Producto</button></a> <a href=""><button>Eliminar Producto</button></a></li>
            
       {% endfor %} 
    </ul>
</div>
{% endblock %}
